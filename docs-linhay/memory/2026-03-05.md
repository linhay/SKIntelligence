# 2026-03-05

## CLI 终端用户安装与分发链路落地

- 背景：`ski` CLI 之前主要通过 `swift run` 开发态使用，缺少终端用户安装入口。
- 关键决策：
  1. 发布资产标准化为 `ski-macos-{arm64|x86_64}.tar.gz` + 对应 `.sha256`。
  2. 发布脚本 `scripts/release_major.sh` 默认自动打包 CLI 资产并附加到 GitHub Release（`RUN_PACKAGE_CLI=1`）。
  3. 提供用户安装脚本 `scripts/install_ski.sh`，默认通过 GitHub Releases latest API 解析版本。
- 代码与文档变更：
  - 新增 `scripts/package_cli.sh`（打包 release 二进制与校验文件）。
  - 新增 `scripts/install_ski.sh`（latest/指定版本安装、架构识别、校验并安装到 prefix/bin）。
  - 更新 `scripts/release_major.sh`（调用打包脚本并上传 `dist/cli` 资产）。
  - 更新 `README.md`（新增终端用户安装说明与 Spec 链接）。
  - 新增 `docs-linhay/features/CLI-Distribution-Install-Spec.md`（BDD 验收场景）。
  - 新增 `docs-linhay/plans/ops/CLI-Install-Runbook.md`（发布与安装 runbook）。
  - 新增 `Tests/SKIntelligenceTests/CLIDistributionScriptsTests.swift`（脚本存在性、关键语义、语法校验）。
- 测试与验证：
  1. 红灯：`swift test --filter CLIDistributionScriptsTests`（新增脚本前失败）。
  2. 绿灯：`swift test --filter CLIDistributionScriptsTests`（4/4 通过）。
3. 功能验证：`scripts/package_cli.sh --arch arm64 --output-dir dist/cli-test` 成功产出压缩包与 sha256。

## CLI Homebrew 安装链路补充

- 决策：
  1. 增加 Homebrew Formula 生成脚本 `scripts/generate_homebrew_formula.sh`，从 `dist/cli/*.sha256` 自动生成 `dist/homebrew/ski.rb`。
  2. `scripts/release_major.sh` 新增 `RUN_HOMEBREW_FORMULA`（默认 `1`），发布时自动生成并上传 `ski.rb` 资产。
  3. README 与运维 runbook 明确 `brew install --formula https://github.com/.../releases/download/<version>/ski.rb` 路径。
- 代码与文档变更：
  - `scripts/generate_homebrew_formula.sh`
  - `scripts/release_major.sh`（Homebrew 变量与流程接入）
  - `README.md`（Homebrew 安装示例）
  - `docs-linhay/features/CLI-Distribution-Install-Spec.md`（新增 Homebrew 场景）
  - `docs-linhay/plans/ops/CLI-Install-Runbook.md`、`docs-linhay/dev/CLI-Distribution-Implementation.md`
  - `Tests/SKIntelligenceTests/CLIDistributionScriptsTests.swift`（新增 Homebrew 断言）
- 验证：
  1. 红灯：`swift test --filter CLIDistributionScriptsTests`（脚本缺失/README 未提及/Homebrew 流程未接入）。
  2. 绿灯：同测试 7/7 通过。
  3. 端到端：`scripts/package_cli.sh --arch all --output-dir dist/cli-test` 成功，随后 `scripts/generate_homebrew_formula.sh --version 2.0.0 --sha-dir dist/cli-test --output dist/homebrew-test/ski.rb` 成功生成 formula。

## 独立 Homebrew Tap 落地（linhay/homebrew-tap）

- 决策：
  1. 采用独立 tap 仓库方案（`linhay/homebrew-tap`），用户安装主路径改为 `brew tap ... && brew install ski`。
  2. 新增 `scripts/sync_homebrew_tap.sh` 用于同步 formula 到 tap 仓库的 `Formula/ski.rb`。
  3. `scripts/release_major.sh` 新增 `EXPORT_FORMULA_TO_REPO` / `HOMEBREW_TAP_*` 环境变量，支持发布时自动同步 tap。
- 执行结果：
  1. `gh repo create linhay/homebrew-tap --public` 成功，仓库已创建。
  2. 首次同步时修复空仓库无 `main` 分支与 trap 变量作用域问题。
  3. 当前 `linhay/homebrew-tap` 已存在 `Formula/ski.rb` 与 `README.md`。
- 代码与文档变更：
  - 新增 `Formula/ski.rb`（可用源码构建安装版本）。
  - 新增 `scripts/sync_homebrew_tap.sh`。
  - 更新 `README.md`（加入 `brew tap linhay/tap https://github.com/linhay/homebrew-tap`）。
  - 更新 `docs-linhay/features/CLI-Distribution-Install-Spec.md`、`docs-linhay/plans/ops/CLI-Install-Runbook.md`、`docs-linhay/dev/CLI-Distribution-Implementation.md`。
  - 更新 `Tests/SKIntelligenceTests/CLIDistributionScriptsTests.swift`（tap 相关断言）。
- 验证：
  1. `swift test --filter CLIDistributionScriptsTests` 9/9 通过。
  2. `gh api repos/linhay/homebrew-tap/contents/Formula/ski.rb --jq '.path'` 返回 `Formula/ski.rb`。

## Homebrew 二进制公式切换（避免安装时下载 Swift）

- 背景：用户安装 `linhay/tap/ski` 时触发 `swift (6.2.4)` 下载，说明 tap 公式仍为源码构建模式。
- 处理：
  1. 先补红灯测试：`Formula/ski.rb` 必须包含 `ski-macos-*.tar.gz`，且不得出现 `depends_on "swift" => :build` 与 `swift build`。
  2. 打包并上传 release 资产到 `2.0.0`：
     - `ski-macos-arm64.tar.gz` / `.sha256`
     - `ski-macos-x86_64.tar.gz` / `.sha256`
  3. 用 `scripts/generate_homebrew_formula.sh` 生成二进制公式并覆盖 `Formula/ski.rb`。
  4. 通过 `scripts/sync_homebrew_tap.sh` 推送到 `linhay/homebrew-tap`（commit `110034a`）。
  5. 文档命令改为显式 `brew install linhay/tap/ski`，避免命中 Homebrew Core 同名包。
- 验证：
  1. `gh release view 2.0.0 --repo linhay/SKIntelligence --json assets` 可见四个 `ski-macos-*` 资产。
  2. `swift test --filter CLIDistributionScriptsTests` 9/9 通过。

## 参考仓补充：pi-mono

- 需求：将 `https://github.com/badlogic/pi-mono` 纳入本地 `docs-linhay/references/`。
- 执行：已克隆到 `docs-linhay/references/pi-mono`。
- 校验：
  1. `git -C docs-linhay/references/pi-mono rev-parse --short HEAD` 返回 `f430dce0`。
  2. `docs-linhay/references/pi-mono/.git` 存在。
- 备注：主仓 `.gitignore` 已忽略 `docs-linhay/references/`，因此该变更为本地参考资源，不会进入当前仓库提交。

## SKI 默认 TUI 聊天入口落地（2026-03-05）

- 决策落地：
  1. `ski`（无子命令）默认进入聊天页。
  2. `ski tui` 保留为显式等价入口。
  3. 默认 profile 为空，不自动连接。
  4. 聊天页仅保留 `/` 选择器作为连接/配置入口，不保留 `Ctrl+O`。
  5. 非 TTY 执行 `ski`/`ski tui` 统一失败并返回退出码 `2`。
- 主要实现：
  1. `Sources/SKICLI/CLI.swift`：根命令注册 `TUICommand` 并在 `run()` 默认走 TUI。
  2. 新增 `Sources/SKICLI/TUI.swift`：
     - `TUICommand` 参数校验
     - `SKITerminalSession`（raw mode + Alt Screen）
     - `SKITUIRuntime`（状态机、Slash 菜单、ACP 连接、差量渲染）
     - `TUIByteParser`（非阻塞键盘事件解析）
  3. `session/update` 文本块流式追加 assistant 消息，支持 Connect/Reconnect/Disconnect/New/Load/Stop Session 等 Slash 动作。
- 测试结果：
  1. `swift test --filter SKICLIProcessTests/testTUI --filter SKICLIProcessTests/testRoot` 通过。
  2. `swift test --filter SKICLITests` 通过。
  3. `swift test --filter SKICLIProcessTests` 全量 103 项通过。

## TUI 冒烟与稳定性修复（2026-03-05）

- 冒烟路径（TTY 真实交互）：
  1. `ski` 启动进入聊天页（Disconnected）成功。
  2. 输入 `/` 自动弹出 Slash 菜单成功。
  3. 菜单执行 `Set Log Level` 成功，状态行更新。
  4. 输入普通 prompt（未连接）时显示 `Not connected, use / to connect` 并追加系统提示。
  5. Slash 执行 `Connect`（未设置 cmd）时显示 `Set Cmd first via / Set Cmd`。
  6. `Esc` 可关闭菜单；`Ctrl+C` 正常退出并恢复终端（`?25h` + `?1049l`）。
- 发现并修复风险：
  - 现象：高频重绘时 `FileHandle.standardOutput.write` 可能抛 `NSFileHandleOperationException`（`Resource temporarily unavailable`）导致崩溃。
  - 修复：`SKITerminalSession.write` 改为底层 `Darwin.write` 循环写入，并处理 `EINTR/EAGAIN/EWOULDBLOCK` 重试，避免异常崩溃。
- 修复后复测：同路径下不再崩溃。

## Shell 链路改造：ACP terminal runtime 接入 SKProcessRunner（2026-03-05）

- 决策：shell/terminal 相关实现统一走 SKProcessRunner，避免继续维护 `Process + Pipe` 分叉路径。
- 实现：
  1. `Package.swift`：`SKIACPClient` target 新增 macOS 条件依赖 `SKProcessRunner`。
  2. `Sources/SKIACPClient/ACPRuntime.swift`：
     - `ACPProcessTerminalRuntime` 非 iOS 分支改为 `SKProcessPipeSession`。
     - `create` 构建 `SKProcessPayload` 并启动会话。
     - `output`/`waitForExit`/`kill`/`release` 适配 session 生命周期。
     - 保留 `allowedCommands/deniedCommands/maxRuntimeNanoseconds` 策略语义。
     - 退出时用 `session.wait()` 返回的 stdout/stderr 做最终收敛，修复尾部输出缺失风险。
- 验证：
  1. `swift test --filter ACPClientRuntimeTests/testProcessTerminalRuntime --filter ACPRuntimePlatformTests` 通过。
  2. `swift test --filter ACPWebSocketClientRuntimeRoundtripTests --filter ACPDomainE2EMatrixTests/testForkPromptContractStdioInProcess --filter ACPDomainE2EMatrixTests/testForkPromptContractWebSocketInProcess` 通过。

## 集成后继续回归（2026-03-05）

- 目标：将 `SKProcessRunner` 锁定到上游提交 `ca6ebc7d3747507d9f3e71e586e241e9ee246541` 后，补齐 CLI 相关回归。
- 结果：全部通过。
  1. `swift test --filter SKIToolShellTests`：2/2 通过。
  2. `swift test --filter SKICLITests`：19/19 通过。
  3. `swift test --filter SKICLIProcessTests`：103/103 通过（含 stdio/ws/tui/会话复用与参数校验场景）。
- 结论：当前依赖切换后，CLI/TUI 与 ACP 客户端流程未出现回归。

## 继续冒烟测试（2026-03-05，集成后）

- TTY 真实交互冒烟（`.build/debug/ski`）：
  1. 默认进入聊天页：`Disconnected`，状态文案正常。
  2. 输入 `/` 弹出 Slash 菜单（Connect/Set Cmd/Set Args/...）。
  3. `Set Cmd` + `Set Args` 后 `Connect` 可成功连通本地 `acp serve --transport stdio`。
  4. 发送 `hello, reply OK` 返回 `[echo] hello, reply OK`，`stop_reason=end_turn`。
  5. `Disconnect` 后再次发送 prompt，正确提示 `Not connected, use / to connect`。
  6. `Ctrl+C` 退出后终端恢复（`?25h` + `?1049l`）。
  7. `Ctrl+O` 无动作（Overlay 入口已移除），`/` 菜单仍可正常打开。
  8. 切换 `transport=ws` 且未设置 endpoint 时，`Connect` 给出 `Set Endpoint first via / Set Endpoint`。
- 非交互冒烟脚本：
  1. `./scripts/acp_stdio_session_reuse_probe.sh`：PASS（第二次复用 stdio session 预期失败，exit=4）。
  2. `./scripts/acp_ws_session_reuse_probe.sh`：PASS（ws 会话复用成功）。
  3. `./scripts/acp_ws_timeout_zero_probe.sh`：PASS（timeout=0 语义正确，end_turn）。
  4. `./scripts/acp_ws_ttl_zero_probe.sh`：PASS（ttl=0 会话过期，session_not_found）。
  5. `./scripts/acp_ws_permission_matrix.sh`：PASS（allow=end_turn / deny=cancelled）。
- 结论：当前 TUI 默认入口、Slash 交互、stdio/ws 关键行为与 ACP session 语义均符合预期，未发现新的回归。

## 继续冒烟测试（第二轮，2026-03-05）

- ACP 回归总脚本（`RUN_CODEX_PROBES=0 ./scripts/acp_regression_suite.sh`）结果：
  1. ws permission matrix：PASS
  2. ws session reuse：PASS
  3. stdio session reuse boundary：PASS
  4. ws ttl-zero boundary：PASS
  5. ws timeout-zero boundary：PASS
  6. summary：`pass=5 fail=0 skipped=2`（codex 可选阶段跳过）
- CLI/TUI 手工补充：
  1. 非 TTY 直接执行 `.build/debug/ski`：按预期退出 `2`，提示使用 `ski acp ...`。
  2. TUI `Set Log Level`：可切换至 `debug`。
  3. TUI `Export Transcript`：可生成 `ski-transcript-*.txt` 文件。
  4. 当会话中尚无消息时，导出文件可为空（当前实现行为）；存在消息时可正确导出（验证样例含 `[Sys] Prompt ignored: not connected`）。
- 结论：当前集成状态下，脚本冒烟与手工关键交互路径继续稳定，无新增回归。

## 继续冒烟测试（第三轮，2026-03-05，含 Codex 可选阶段）

- 执行：`./scripts/codex_acp_smoke.sh 3`
  - 结果：`success=0/3`，3 次均失败。
  - 关键错误：`UsageLimitExceeded`（错误信息提示可在 `Mar 10th, 2026 10:24 AM` 后重试）。
  - 现象补充：仍会收到 `session_update(available_commands_update)`，但无 `prompt_result`。
- 执行：`./scripts/codex_acp_permission_probe.sh`
  - 结果：在 `allow` 阶段即失败（exit=4），`deny` 阶段未执行。
  - 根因同上：`UsageLimitExceeded`。
- 执行：`./scripts/codex_acp_multiturn_smoke.sh`
  - 结果：失败（exit=4），未产出期望的 2 条 `prompt_result`。
  - 根因同上：`UsageLimitExceeded`。
- 执行：`RUN_CODEX_PROBES=1 ./scripts/acp_regression_suite.sh`
  - 必跑 5 阶段：全部 PASS。
  - 可选 2 阶段（codex permission / multiturn）：均 WARN（重试后仍 exit=4）。
  - suite 汇总：`pass=5 fail=0 warn=2 skipped=0 requiredPassed=true ciRecommendation=pass_with_warnings`。
- 结论：当前代码链路稳定；codex 可选冒烟受外部账号配额限制，不属于本仓代码回归。

## 切换到 MLX 模型后的冒烟（2026-03-05）

- 切换目标：`acp serve --model-provider mlx`，模型使用 `mlx-community/Qwen2.5-0.5B-4bit`。
- 环境准备与模型可用性验证：
  1. 执行 `scripts/mlx_e2e_prepare.sh --run --model-id mlx-community/Qwen2.5-0.5B-4bit --timeout-seconds 240 --temperature 0`。
  2. 结果：`MLXClientDeterminismE2ETests` 通过（文本 determinism 用例通过，VLM 用例按预期 skip）。
- ACP(MLX) 端到端验证：
  1. 单轮 `acp client connect --transport stdio` + `serve --model-provider mlx` 成功，产出 `prompt_result:end_turn`。
  2. 多轮（两个 `--prompt`）在 `--mlx-disable-tool-call` 下通过：
     - `PROMPT_RESULTS=2`
     - `SESSION_IDS=1`（同 session 连续多轮）
- 观察与结论：
  1. 首次使用 MLX 时，若 client `--request-timeout-ms` 过短可能出现 `session/prompt` 超时（模型加载阶段）。
  2. 关闭 client 超时或适当放大超时后链路稳定。
  3. 当前代码链路可切换到 MLX 继续冒烟，不依赖 Codex 额度。

## 继续冒烟（第四轮，MLX 深入）

- 新增覆盖：`WS + MLX`、`TUI + MLX`、permission 探针。
- 结果摘要：
  1. `WS + echo` 基线正常（`connect-ws` 可拿到 `prompt_result:end_turn`）。
  2. `WS + MLX` 复现异常：
     - client 报 `Could not connect to the server`（exit=4）。
     - server 日志出现 `Error: Unsupported websocket frame opcode` 并退出。
  3. `TUI + MLX(stdio)`：
     - 默认 `--request-timeout-ms=60000` 时，长响应下可能 `session/prompt timeout`。
     - 设置 `--request-timeout-ms 0` 后可稳定完成，`Prompt finished: end_turn`。
  4. `stdio + MLX` 多轮（`--mlx-disable-tool-call`）仍可通过（此前验证 `PROMPT_RESULTS=2` / `SESSION_IDS=1`）。
  5. `permission-decision deny` 在 `stdio + MLX` 下未出现 `cancelled`，实测为 `end_turn`（结合输出看更像模型在拒绝工具后继续自然回复）。
- 当前判断：
  1. MLX 主可用路径是 `stdio`（含 TUI）；
  2. `WS + MLX` 需要单独排障（frame opcode 异常）。

## 继续测试（第五轮，聚焦 WS/MLX）

- 自动化 WebSocket 组回归：
  - 执行：
    - `ACPWebSocketRoundtripTests`
    - `ACPWebSocketPermissionRoundtripTests`
    - `ACPWebSocketMultiClientTests`
    - `ACPWebSocketRoutingTests`
    - `ACPWebSocketClientDeterministicReconnectTests`
  - 结果：`14/14` 全通过。
- 实机探针：
  1. `WS + echo`：通过（`prompt_result:end_turn`）。
  2. `WS + MLX`（`--mlx-disable-tool-call`）：稳定失败；client 报 `Could not connect to the server`，server 日志报 `Unsupported websocket frame opcode`。
  3. `stdio + MLX`：本轮表现不稳定，出现 `Request timed out: session/prompt`（180s 超时）。
  4. `stdio + echo`：通过（`prompt_result:end_turn`），排除通用 ACP 链路问题。
- 风险结论：
  1. 当前问题集中在 MLX 路径（尤其 `WS+MLX`），非 WebSocket 基础链路全面失效。
  2. 在修复前，推荐继续使用 `echo` 或 MLX 的受控 stdio 路径进行基础联调。

## WS + MLX 修复（2026-03-05）

- 问题：`acp serve --transport ws --model-provider mlx` 下，客户端连接长耗时场景会出现 server 侧 `Unsupported websocket frame opcode` 并导致连接失败。
- 修复：
  1. `Sources/SKIACPTransport/WebSocketServerTransport.swift`
     - `decodeMessage` 改为可返回 `nil`；
     - 对非 text/binary opcode（控制帧/未知帧）不再抛 fatal，改为忽略；
     - `onReceive` 在 `decodeMessage == nil` 时继续监听而非中断 receive loop。
  2. 新增回归测试（`Tests/SKIntelligenceTests/ACPWebSocketRoundtripTests.swift`）：
     - `testWebSocketServerIgnoresHeartbeatControlFramesDuringSlowPrompt`
     - `testWebSocketServerSurvivesClientCloseControlFrame`
- 验证：
  1. WebSocket 自动化组回归（Roundtrip/Permission/MultiClient/Routing/DeterministicReconnect）`14/14` 全通过。
  2. `WS + echo` 探针通过（`prompt_result:end_turn`）。
  3. `WS + MLX` 探针：不再出现 `Unsupported websocket frame opcode`；在当前模型速度下表现为 `session/prompt` 超时（可通过调整 timeout 继续优化）。
- 结论：本次修复已消除 WS+MLX 的 frame opcode 崩溃，剩余为模型推理耗时治理问题。

## 进入修复（WS 控制帧回归确认，2026-03-05）

- 执行：
  1. `swift test --filter ACPWebSocket`（19 过 / 1 跳过）
  2. 本地冒烟：`.build/debug/ski acp serve --transport ws --model-provider mlx --mlx-model-id mlx-community/Qwen2.5-0.5B-4bit` + `acp client connect-ws --prompt "hello, reply OK" --json`
- 结果：
  1. `WS + MLX` 不再出现 `Unsupported websocket frame opcode`。
  2. 客户端收到 `prompt_result`，`stopReason=end_turn`。
  3. 当前剩余风险转为模型响应质量/耗时调优，不是 websocket 控制帧导致的连接崩溃。

## 进入修复（MLX 首轮超时兜底，2026-03-05）

- 现象：`acp client connect-ws` 默认 `request-timeout-ms=60000`，在 MLX 首轮加载时易触发 `Request timed out: session/prompt`。
- 决策：将“连接类命令”和 TUI 的默认请求超时提升到 `300000ms`（5 分钟），`stop` 类命令维持 `60000ms`。
- 代码变更：
  1. `ACPClientConnectCommand` / `connect-stdio` / `connect-ws` 默认 `requestTimeoutMS = 300_000`。
  2. `TUICommand` 与 `runSKIDefaultChatMode` 默认 `requestTimeoutMS = 300_000`。
  3. `stop` / `stop-stdio` / `stop-ws` 保持 `60_000`。
- 测试：
  1. 新增进程测试：`testClientConnectWSHelpShowsFiveMinuteDefaultRequestTimeout`
  2. 新增进程测试：`testTUIHelpShowsFiveMinuteDefaultRequestTimeout`
  3. `swift test --filter SKICLIProcessTests`：105/105 通过。
- 冒烟：`WS + MLX`（未显式传 `--request-timeout-ms`）返回 `prompt_result:end_turn`，未出现 `session/prompt` 超时。

## 再次冒烟（2026-03-05）

- 执行：
  1. `RUN_CODEX_PROBES=0 ./scripts/acp_regression_suite.sh`
  2. `WS + MLX` 实机探针（默认 `connect-ws` 参数）
  3. `stdio + MLX` 实机探针（默认 `connect-stdio` 参数）
- 结果：
  1. 回归总脚本必跑阶段 `5/5 PASS`。
  2. `WS + MLX` 返回 `prompt_result:end_turn`（exit=0）。
  3. `stdio + MLX` 返回 `prompt_result:end_turn`（exit=0）。
- 结论：当前默认超时调整后，MLX 主链路冒烟稳定；未复现 `session/prompt timeout` 与 `Unsupported websocket frame opcode`。

## 脚本化复测（2026-03-05）

- 执行：
  1. `RUN_CODEX_PROBES=0 ./scripts/acp_regression_suite.sh`
  2. `./scripts/test_acp_gate.sh`
- 结果：
  1. `acp_regression_suite` 必跑阶段 `5/5 PASS`（fail=0, warn=0）。
  2. `test_acp_gate.sh` 总计 `302` 项（`1` skip）全部通过，无失败。
- 结论：当前 ACP/CLI/WebSocket 回归在脚本入口下稳定。

## 发布与 Brew 安装（2026-03-05）

- 发布：
  1. 新增发布说明：`docs-linhay/plans/ops/Release-2.0.2-GitHub-Short.md`
  2. 执行：`DRY_RUN=0 RUN_TESTS=0 RUN_BUILD=0 EXPORT_FORMULA_TO_REPO=1 scripts/release_major.sh release 2.0.2 docs-linhay/plans/ops/Release-2.0.2-GitHub-Short.md`
  3. 结果：
     - tag `2.0.2` 已推送
     - GitHub Release 已创建：`https://github.com/linhay/SKIntelligence/releases/tag/2.0.2`
     - release 资产包含：`ski-macos-{arm64,x86_64}.tar.gz/.sha256`、`ski.rb`、`skintelligence.skill`
     - homebrew tap 已同步到 `linhay/homebrew-tap@main`（commit `e924cf5`）
- 安装验证（brew）：
  1. 刷新本地 tap：`git -C $(brew --repository)/Library/Taps/linhay/homebrew-tap pull --ff-only`
  2. 重装：`brew reinstall linhay/tap/ski`
  3. 结果：`linhay/tap/ski: stable 2.0.2`，`/opt/homebrew/bin/ski -> ../Cellar/ski/2.0.2/bin/ski`
  4. 命令验证：`ski --help` 输出 `OVERVIEW: SKIntelligence CLI`

## TUI 中文显示回归定位（2026-03-05）

- 现象：用户反馈 `ski tui` 显示异常（中文输入/消息不正常）。
- 复现：
  1. Homebrew 已安装版本 `/opt/homebrew/bin/ski`（Cellar `2.0.2`）下，PTY 自动化输入 `你好` 后界面无中文（`HAS_NI=false`）。
  2. 仓库当前修复版（本地构建）同路径冒烟，`你好` 与 `你好，回复OK` 及 `[echo]` 回包在 TUI 中可见。
- 根因：`2.0.2` 发布资产未包含 UTF-8 输入解析与宽字符列宽渲染修复。
- 结论：需要发布包含 TUI 修复的新版本（建议 `2.0.3`）并同步 Homebrew tap，避免用户继续安装到旧行为。

## 发布 2.0.3（TUI 中文修复，2026-03-05）

- 提交：`8cd6a14`（`fix(tui): support utf8 input and wide-char rendering`）。
- 发布：`DRY_RUN=0 RUN_TESTS=0 RUN_BUILD=0 EXPORT_FORMULA_TO_REPO=1 ./scripts/release_major.sh release 2.0.3 docs-linhay/plans/ops/Release-2.0.3-GitHub-Short.md`
- 结果：
  1. tag：`2.0.3` 已推送。
  2. release：`https://github.com/linhay/SKIntelligence/releases/tag/2.0.3`。
  3. tap：`linhay/homebrew-tap@main` 已更新（commit `99ac6bc`）。
- 安装验证：
  1. `brew reinstall linhay/tap/ski` -> `stable 2.0.3`。
  2. PTY 冒烟（brew 二进制）下，`ski tui` 可显示中文输入 `你好`。
  3. `acp serve(ws)+tui` 冒烟可见 `[echo]` + 中文回包。
