# 2026-03-06

## CLI 版本与 TUI 显示修复

- 需求：
  1. `ski` 支持 `version` 查询。
  2. 修复 TUI 在部分终端尺寸获取异常时显示不正确问题。
- 决策：
  1. 根命令新增 `--version`（ArgumentParser `version` 配置）与 `version` 子命令。
  2. 新增 `SKICLIVersion`：优先 `SKI_VERSION` 环境变量；其次解析 Homebrew Cellar 路径版本；再尝试 Git tag；最后回退 `dev`。
  3. 新增 `TUITerminalSizing`：winsize 无效时回退 `COLUMNS/LINES`，再回退 `80x24`，并保持最小 `20x8`。
- 测试：
  1. `SKICLIProcessTests` 新增 `--version` 与 `version` 子命令用例。
  2. 新增 `TUITerminalSizingTests` 覆盖 winsize/env/default 三类路径。
  3. 回归通过：`SKICLIProcessTests + TUIByteParserTests + TUITerminalSizingTests` 共 `113` 项。
- 冒烟：
  1. `.build/debug/ski --version` 与 `ski version` 输出 `2.0.3`。
  2. `acp serve(ws)+tui` 中文 prompt/回包显示正常。

## 直接安装脚本（源码构建）

- 新增脚本：`scripts/install_ski_source.sh`
  - 支持从本地源码目录或 GitHub 仓库直接构建并安装到指定前缀。
  - 默认安装到 `~/.local/bin/ski`，可通过 `--prefix` 覆盖。
- 版本可读性：
  - 安装脚本会写入 sidecar 文件 `ski.version`。
  - `SKICLIVersion` 新增 sidecar 读取逻辑，保证源码安装后 `ski --version` 可显示稳定版本而非 `dev`。
- 验证：
  1. `swift test --filter CLIVersionTests --filter CLIDistributionScriptsTests` 通过。
  2. 线测 `scripts/install_ski_source.sh --source-dir "$PWD" --prefix /tmp/ski-local --config debug` 后，`/tmp/ski-local/bin/ski --version` 输出 `2.0.3`。
