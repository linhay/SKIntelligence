## 2026-02-25 iOS v17 编译兼容收口
- 决策：iOS/tvOS/watchOS 平台对不支持的进程能力统一降级，不再尝试编译或运行 `Foundation.Process` 路径。
- 实施：`ACPProcessTerminalRuntime` 增加平台分支；移动平台 `terminal/*` 直接抛 `ACPTransportError.unsupported`，桌面平台保留原实现。
- 保障：新增 `ACPRuntimePlatformTests`，并与 `ACPTransportPlatformTests` 一起验证平台语义。
- 结果：`xcodebuild -scheme SKIntelligence-Package -destination 'generic/platform=iOS Simulator' -configuration Debug build` 构建通过（iOS 17 simulator 目标）。
- 里程碑：发布版本 `1.3.14`，已完成 `main` 推送、tag 推送与 GitHub Release。
- 发布链接：https://github.com/linhay/SKIntelligence/releases/tag/1.3.14
